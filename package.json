{
  "name": "opencode-hwtracker",
  "version": "0.1.0",
  "description": "opencode plugin: records local server hardware when model output is slow",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/index.ts --target bun --format esm --outfile dist/opencode-hwtracker.js"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/node": "^20.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.5.0"
  }
}
